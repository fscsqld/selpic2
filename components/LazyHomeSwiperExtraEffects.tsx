/**
 * Extra Swiper effects for Admin CMS hero settings (cube / coverflow / flip).
 * Loaded only when heroSliderSettings.effect is not fade — keeps default home chunk slim.
 */
'use client'

import { EffectCube, EffectCoverflow, EffectFlip } from 'swiper/modules'
import 'swiper/css/effect-cube'
import 'swiper/css/effect-coverflow'
import 'swiper/css/effect-flip'

export { EffectCube, EffectCoverflow, EffectFlip }
