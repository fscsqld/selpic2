---
description: Storefront mobile performance (PageSpeed / Lighthouse) — apply on every storefront/admin UI change
alwaysApply: true
---

# Storefront mobile performance (PageSpeed / Lighthouse)

**Baseline** (Sep 9 2026 **1:33** PM GMT+10): Perf **51**, FCP 3.3s, LCP 8.4s, TBT 420ms, SI 7.7s, CLS 0, ~14.5 MiB.  
**Post batch-1** (Sep 10 **10:11** AM): Perf **67**, FCP 2.4s, LCP 5.8s, TBT 50ms, SI 9.3s, CLS 0, ~14.2 MiB.  
**Prior** (Sep 10 **4:58** PM): Perf **74**, LCP **4.8s**, TBT 100ms, SI 8.8s, A11y **92**, payload **~4.8 MiB**.  
**Prior lab** (Sep 11 **3:29** PM GMT+10): Perf **78**, FCP **1.1s**, LCP **4.4s**, TBT **30ms**, SI **7.3s**, CLS 0.002, payload **~4.8 MiB**, A11y **92**.  
**Prior lab** (Sep 12 **9:42** PM GMT+10): Perf **75**, FCP **1.1s**, LCP **4.8s**, TBT **80ms**, SI **7.1s**, CLS **0.03**, payload **~3.2 MiB**, A11y **100**.  
**Prior lab** (Sep 12 **10:11** PM GMT+10): Perf **69**, FCP **1.1s**, LCP **6.1s**, TBT **120ms**, SI **8.5s**, CLS **0.002**, payload **~3.2 MiB**, A11y **100**.  
**Latest lab** (Sep 16 **10:01** AM GMT+10): Perf **77**, FCP **1.1s**, LCP **4.4s**, TBT **40ms**, SI **7.8s**, CLS **0.002**, payload **~3.2 MiB** (3,264 KiB). A11y **100**, BP **100**, SEO **100**, Agentic **2/2**. CrUX **No Data**. Weights: FCP +10, LCP +10, TBT +30, CLS +25, SI +2.  
Full mapping: `docs/storefront-mobile-lighthouse-2026-09-09.md`. Goal: LCP ≤2.5–4s lab without Hero redesign.

## Invariant

Mobile first paint and LCP must not wait on **unused** CDN fonts, **no-store of hashed static assets**, or **entire-page** client bundles. Hero design stays protected — optimize **how** assets load.

**Hero title (learned 2026-09-12 evening):** Owner wants **text only** over the hero video/image — **no** filled scrim panel (`bg-black/*` rounded box) behind title/subtitle. Any dark box makes the media look muddy. Prefer white copy + `drop-shadow` / `text-shadow` for readability. Do **not** re-add a title background panel for A11y/PSI alone.

**Category images look dark (re-learned 2026-09-16):** Not a broken CMS upload — intentional overlays for white title/emoji text.

| Where | Overlay | Why |
|-------|---------|-----|
| **Admin → Category Management** card preview | `bg-black/20` full tile (`CategoryManager.tsx`) | Preview mirrors storefront: white title/description readable on light photos |
| **Storefront homepage** category cards | gradient `from-black/28 via-black/10 to-transparent` (`app/page.tsx`) | **Lightened again 2026-09-16** (was `/40` `/15` `/5`) — brighter CMS photos |

Do **not** remove these scrims without owner OK — photos will look brighter but white text may fail contrast. Category card scrims are **separate** from the hero title “no fill panel” rule.

**PSI score vs header logo (learned 2026-09-12 10:11):** Owner re-uploaded a smaller transparent header logo (~16.5 KB) then saw Perf **75 → 69**. **Not caused by the logo.** Payload stayed **~3,247 → 3,249 KiB** (flat). Score fell because **LCP 4.8s → 6.1s** (weight +8 → +3) and **SI 7.1s → 8.5s**, plus TBT **80 → 120ms** — classic **lab variance / Slow-4G hero timing**. LCP element is **hero slide 1 video** (`hero-banner.mp4` ~**multi‑MB**) + poster WebP, not the sticky header logo (~17 KB). Do **not** blame CMS logo swaps for Perf swings of this size; re-run PSI or trim hero MP4 instead.

**External “Next Image + purge logo.png + Supabase preconnect” brief (learned 2026-09-12):** **Not the root cause** of Perf 69 / LCP 6.1s. Hero slide 1 is **video** (~1.7 MB); intentional `<img>` + `fetchPriority` / onError already used — do **not** rewrite Hero to `next/image` for this. Legacy `/images/logo.png` (~91 KB favicon/fallback) is not LCP. Supabase `preconnect` is optional micro-opt only. See `docs/storefront-mobile-lighthouse-2026-09-09.md` § Sep 12 external brief.

**External “defer hero video + prioritize LCP image” brief (learned 2026-09-12):** Diagnosis partly right (MP4 can contend on Slow 4G). **Already shipped:** mobile poster-first (~2.8s/idle), `preload="metadata"`, LCP = fallback WebP + `fetchPriority="high"` + `<link rel="preload" as="image">`. Do **not** re-do as Next `<Image fill>` refactor. **>85** needs **smaller admin MP4**, not this brief alone. See doc § defer video brief.

## Top remaining lab insights (after 10:01 Sep 16)

| Insight | Notes |
|---------|--------|
| Image delivery (~**608 KiB**) | Up vs ~407 @ 10:11 PM claim — residual CMS/hero assets |
| Cache lifetimes (~**2,006 KiB**) | Mostly **3rd-party** TTL (Supabase/CDN) |
| Render-blocking (~**670 ms**) | Similar class to prior ~610–720ms |
| LCP **4.4s** | Back to best lab class (was 6.1s @ 10:11 PM) — LCP weight **+10** again |
| SI **7.8s** | Better than 8.5s @ 10:11 PM; still above “good” |
| TBT **40ms** | Strong (was 120ms) |
| CLS **0.002** | Held |
| Payload **~3.2 MiB** | Flat vs Sep 12 |
| A11y **100** | Hold |
| Perf **77** | Recovered from 69; near prior peak 78 — confirms 69 was lab noise, not logo |

**PSI image delivery ~608 KiB / Supabase list (learned 2026-09-16):** Flags are mostly **full-resolution CMS WebP served into smaller CSS boxes** (category ~400px display vs ~800–1000px files; header logo 584×172 vs ~136×40). Supabase URLs are **pass-through** (Unsplash-only downsize in `optimizeStorefrontImageUrl`). `/images/logo.png` ~89 KiB is **favicon/static**, not the CMS header WebP. See `.cursor/rules/psi-image-delivery-oversized-cms.mdc`.

**Header CMS logo asset (learned 2026-09-12 evening):** Owner’s preferred company logo for Admin → Logo Image Management is the transparent-bg SELPIC mark (~**16.5 KB** WebP, **584×172**, **has alpha**). Re-upload fixes opaque-plate “box”. Prefer PNG/WebP **with transparency** (or SVG). Cloud `usage=header-logo` required for multi-device sync.

## Do

- **Fonts:** Root Inter only (`display: swap`, preload). Customize/stamp fonts stay off `/` (`GoogleFontsLinks`).
- **JS:** `dynamic` / deferred Swiper (`LazyHomeSwiper`); avoid new whole-route `'use client'` weight on `/`.
- **Images:** Prefer CMS WebP under budgets; product AI `encodeStorefrontWebp`; Unsplash via `optimizeStorefrontImageUrl`.
- **Video hero:** On ≤1023px / Save-Data, show WebP fallback first and defer MP4 (~2.8s / idle); preload LCP href uses video `fallbackImage`. Desktop still loads video promptly. Admin smaller MP4 remains helpful.
- **Cache:** `/_next/static` immutable; `/images`, `/media`, logo/apple-touch = 1d + SWR; public site-config `s-maxage=60` SWR 300. HTML home may stay `no-store`. Do not blanket-`no-store` public CMS/catalog.
- **A11y:** Icon labels + captions; hero title = **no fill panel** — text + shadow only so video stays sharp; avoid `text-transparent`/`bg-clip-text` for body copy; footer `text-gray-300` on `bg-gray-900`. Hold A11y **100**.
- **3rd parties:** No new render-blocking scripts in root layout.
- **PSI reading:** Compare **LCP/SI/payload** across runs before attributing score drops to small CMS assets (header logo).

## Do not

- Redesign Hero logo / HOT ITEM / Framer / brand gradients.
- Re-add a filled hero title scrim panel (`bg-black/…` box behind SELPIC/subtitle) — owner prefers clear video + text-only (2026-09-12).
- Attribute large Perf drops to header-logo KB changes when payload is flat and LCP moved (2026-09-12 10:11).
- Treat generic “Hero must use next/image + delete logo.png + add Supabase preconnect” briefs as the LCP root cause — **learned false** for SELPIC (video hero; see doc).
- Pull accounting-sandbox into storefront webpack.
- Treat only desktop; assume Moto G / Slow 4G.
- Claim wins from older baselines — use latest PSI time (**10:01** Sep 16).

## When touching `/` or root layout / next.config headers

1. Re-read this rule + `homepage-protection.mdc`.
2. After meaningful perf work: PSI mobile — note vs **10:01** (77 / 4.4s LCP / TBT 40ms / SI 7.8s / ~3.2 MiB / A11y 100). Expect some LCP noise; primary lever remains smaller hero MP4.
